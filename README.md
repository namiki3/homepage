# TODOボット Web版 - セットアップガイド

## 📦 必要なファイル

ダウンロードしたファイル:
- `server.js` - サーバーファイル（Slackボットを変換済み）
- `chat.html` - チャットUI
- `package.json` - パッケージ設定
- `README.md` - このファイル

**追加で必要**: あなたのSlackボットプロジェクトから `index.js` をコピーしてください。

## 🚀 セットアップ手順

### 1. ファイルの配置

```
todo-bot-web/
├── server.js          ← ダウンロード済み
├── chat.html          ← ダウンロード済み
├── package.json       ← ダウンロード済み
├── index.js           ← あなたのSlackボットからコピー
└── public/
    └── chat.html      ← chat.htmlをここに移動
```

以下のコマンドを実行:

```bash
# publicディレクトリを作成
mkdir public

# chat.htmlをpublicに移動
mv chat.html public/

# あなたのindex.jsをコピー（パスは適宜変更）
cp /path/to/your/slack-bot/index.js ./
```

### 2. 依存パッケージのインストール

```bash
npm install
```

### 3. サーバー起動

```bash
npm start
```

### 4. ブラウザでアクセス

```
http://localhost:3000/chat.html
```

## 💡 使い方

チャット画面で以下のコマンドを入力:

- `add 買い物に行く` - TODOを追加
- `done 買い物に行く` - TODOを完了
- `del 買い物に行く` - TODOを削除
- `list` - TODOリストを表示
- `donelist` - 完了リストを表示

**クイックコマンドボタン**も使えます！

## 🔧 変換内容

元のSlack Boltコード:
```javascript
app.message(/add (.+)/i, ({context, say}) => {
  const taskName = context.matches[1].trim();
  todo.add(taskName);
  say(`追加しました: ${taskName}`);
});
```

変換後:
```javascript
const addMatch = text.match(/^add (.+)/i);
if (addMatch) {
  const taskName = addMatch[1].trim();
  todo.add(taskName);
  return `✅ 追加しました: ${taskName}`;
}
```

## 🐛 トラブルシューティング

### `Cannot find module './index.js'`
→ Slackボットの`index.js`をこのディレクトリにコピーしてください

### `ENOENT: no such file or directory, open 'public/chat.html'`
→ `chat.html`を`public/`ディレクトリに移動してください

### ポート3000が使用中
```bash
# 使用中のプロセスを確認
lsof -i :3000

# 別のポートで起動
PORT=8080 npm start
```

## 📁 正しいディレクトリ構造

```
todo-bot-web/
├── server.js
├── package.json
├── index.js           ← 重要！
├── node_modules/      ← npm install後に作成
└── public/
    └── chat.html
```

## ✅ 動作確認

1. `npm start`を実行
2. `http://localhost:3000/chat.html`にアクセス
3. `list`と入力して送信
4. ボットが応答すれば成功！

---

問題が解決しない場合は、エラーメッセージを教えてください！
# TODObot-web
# ホームページ
# homepage
